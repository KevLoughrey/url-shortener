# Render deployment configuration
# This file tells Render how to build and run your Go backend

# Build command
buildCommand = "go build -o main ."

# Start command  
startCommand = "./main"

# Environment
[env]
GO_VERSION = "1.24"
